{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/api/now/:snpath*", "destination": "/api/proxy?snpath=:snpath*" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
